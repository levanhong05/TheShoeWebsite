@model IEnumerable<TheShoeWebsite.Models.Size>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Description
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "SizeEdit", new { id = item.SizeId }) |
            @Html.ActionLink("Details", "SizeDetails", new { id=item.SizeId }) |
            @Html.ActionLink("Delete", "SizeDelete", new { id = item.SizeId })
        </td>
    </tr>
}

</table>
